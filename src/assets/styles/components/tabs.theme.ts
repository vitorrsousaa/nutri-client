import { StyleConfig } from '@chakra-ui/theme-tools';

export const tabsTheme: StyleConfig = {
  baseStyle: {
    tab: {
      fontWeight: 'semibold',
      _selected: {
        color: '#59BD5A',
      },
    },
  },
};
