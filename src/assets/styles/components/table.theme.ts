import { StyleConfig } from '@chakra-ui/theme-tools';

export const tableTheme: StyleConfig = {
  variants: {
    simple: {
      th: {
        borderColor: '#ccc',
      },
      td: {
        borderColor: '#ccc',
      },
    },
  },
};
